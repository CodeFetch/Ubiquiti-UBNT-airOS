Index: hostapd-2.1/src/tls/x509v3.c
===================================================================
--- hostapd-2.1.orig/src/tls/x509v3.c
+++ hostapd-2.1/src/tls/x509v3.c
@@ -1847,11 +1847,15 @@ int x509_certificate_chain_validate(stru
 		if (chain_trusted)
 			continue;
 
-		if (!disable_time_checks &&
-		    ((unsigned long) now.sec <
-		     (unsigned long) cert->not_before ||
+#ifdef NO_TIMESTAMP_CHECK
+		disable_time_checks = 1;
+#endif
+                /* UBNT: allow cert date in the future, but still check for expired */
+		if ((!disable_time_checks &&
+		    (unsigned long) now.sec <
+		     (unsigned long) cert->not_before) ||
 		     (unsigned long) now.sec >
-		     (unsigned long) cert->not_after)) {
+		     (unsigned long) cert->not_after) {
 			wpa_printf(MSG_INFO, "X509: Certificate not valid "
 				   "(now=%lu not_before=%lu not_after=%lu)",
 				   now.sec, cert->not_before, cert->not_after);
